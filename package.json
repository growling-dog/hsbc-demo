{
  "name": "hsbc-demo-new",
  "version": "1.0.0",
  "description": "This is a GraphQL simple assignment and this is used for HSBC demo.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist/ && node dist/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "graphql": "^0.13.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-plugin-transform-object-rest-spread": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
